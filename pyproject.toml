[project]
name = "comfyui-modeldownloader-sg"
description = "Detects missing models in ComfyUI workflows and downloads them from Hugging Face (priority) or Civitai using API tokens."
version = "1.0.0"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = ["aiohttp>=3.8.0", "requests>=2.28.0"]

[project.urls]
Repository = "https://github.com/yourusername/ComfyUI-ModelDownloader_SG"

[tool.comfy]
PublisherId = "sagar"
DisplayName = "ComfyUI-ModelDownloader_SG"
Icon = ""
